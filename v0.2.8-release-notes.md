## v0.2.8 (2026-07-31)

### ✨ 新增

- **注释设置板块**：在「导出生词本」板块上方新增「注释设置」板块，包含以下配置项：
  - 加入生词本时同步添加到注释（总开关，不勾选时下方全部灰色禁用）
  - 自动翻译注释（保存翻译至注释评论 / 保存翻译至注释正文，下拉菜单选择）
  - 翻译保存位置（翻译置于注释正文前 / 翻译置于注释正文后，仅「注释正文」时生效，下拉菜单选择）
  - 分隔符（原文与翻译之间）
  - 标注方式（高亮文本 / 下划线文本）
  - 标注颜色（颜色框 + `#ffd400` 格式输入框）
  - 翻译后自动为注释添加标签（标签名称默认「单词」）
- **注释创建逻辑**：加入生词本时自动在 PDF 上创建 Zotero 注释：
  - 支持 hover（悬停翻译）和 selection（选区弹窗）两种场景
  - hover 场景：通过 PDF.js viewport 将 DOM Range 坐标转换为 PDF 用户空间坐标
  - selection 场景：直接使用 Zotero reader 事件提供的 PDF 坐标
  - 调用 `Zotero.Annotations.saveFromJSON` 持久化注释
  - 支持高亮/下划线两种标注方式，支持翻译保存至评论或正文
  - 支持自动为注释添加标签
  - 参考 zotero-ai-sidebar 的注释流程实现
- **导出路径选择目录按钮**：导出路径输入框后新增「选择目录」按钮，点击弹出文件资源管理器选择目录，路径自动填入输入框（参考 tara 插件实现）
- **隐藏便签图标**：按需隐藏 PDF 中高亮/下划线/图片注释的便签图标：
  - 隐藏范围：「仅隐藏单词文本的便签」（仅隐藏本插件创建的注释，按注释 ID 精确识别）/「仅隐藏全部文本的便签」（隐藏所有高亮/下划线/图片便签）
  - 独立便签图标：独立控制，不受隐藏范围限制
  - 注释 ID 自动跟踪与清理（删除/回收站自动清理 + 打开时对照自愈），无启发式误判

### 🔧 改进

- **高亮颜色框改造**：
  - 颜色框去除左侧空白（增加 `padding:0; border:none; cursor:pointer;` 样式，参考 zotero-dual-title）
  - `R`/`G`/`B` 三个数字输入框合并为一个 `Color` 十六进制输入框（格式 `#ffd400`，参考 zotero-style 标注颜色）
  - `A` 更名为「透明度」，默认值 100%
  - 默认高亮颜色为 `rgba(255,233,79,1.0)`（R:255 G:233 B:79 A:100%）
- **「选中文字时禁用悬停」改为「划词时禁用悬停弹窗」**（默认勾选）：
  - 基于「划词过程 + 选区工具栏 / Translate 划词翻译弹窗」精确检测（穿透 shadow DOM），不再按选区位置启发式判断
  - 划词过程中抑制悬停翻译弹窗与取词高亮；划词完成后保留取词高亮、仅抑制悬停翻译弹窗
  - 已高亮/划线注释的文本可正常悬停取词，无需点击页面恢复
- **板块标题间距**：各板块标题（`h3`）与上一板块最后一行之间保持一行间距（`margin-top: 16px`）
- **翻译弹窗最小宽度**：`min-width` 由 `120px` 调整为 `100px`，缩窄弹窗最小宽度下限

### 🐛 修复

- 透明度默认值修正为 100%（原为 45%）
- 翻译保存位置从勾选项改为下拉菜单，与「取词触发方式」风格一致
- 分隔符标签不再强行对齐，输入框紧跟标签在同一行
- 标签名称输入框与其他输入框对齐
- 插件创建的注释标签改为覆盖式设置（`setTags`），不受第三方插件（Actions and Tags for Zotero / zotero-style 颜色标签联动）自动附加标签污染
- 修复 hidden 便签图标功能的多项问题（V2 闭包失效、boolean pref 字符串化、颜色匹配误判、标签启发式误判等）
