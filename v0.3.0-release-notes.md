## v0.3.0 (2026-08-04)

定位架构重构：取词（A 通道 DOM 命中）与定位（C 通道 PDF 字符 rect）分离的**渐进增强**体系。本版本从 v0.2.9 起累计包含两阶段工作：C 通道从零搭建打通（含 XPC 桥接攻坚）与定位精度/稳定性系列修复。

### 一、C 通道渐进增强定位（架构核心）

- **C 通道字符级定位器（wordLocator.ts）**：基于 Zotero 9 官方 `pdfDocument.getPageData({pageIndex})` 逐字符数据（`c/rect/baseline/fontSize/rotation/spaceAfter/lineBreakAfter`，已在 Zotero-9.0.6 官方 omni.ja 源码确认），按页惰性构建 + 每 reader 每页缓存；就绪后高亮、弹窗锚定、批注几何全部跟随字符 rect，根治 textLayer 错位 / 字体 fallback 导致的几何偏差。
- **取词与定位分离**：A 通道 `caretPositionFromPoint` 同步取词文本（1ms，永不替换）；C 通道负责精确定位（2-10ms）。C 不可用/未就绪时回退 A 的 DOM 几何，高亮永不缺席。
- **XPC 跨边界桥接（8 轮攻坚，终版方案）**：chrome 上下文无法直接 `await` iframe 的 `getPageData`（返回对象含 DOM 引用，XPC 深克隆必炸）。终版：在 iframe 原生 window 用 `eval` 注入桥接函数——函数在 iframe 内完成 `getPageData` + chars 精简 + `JSON.stringify`，结果写入 iframe 全局字符串属性，chrome 侧 void 调用后轮询读取。**跨边界只传字符串，物理上不可能克隆失败**。

### 二、定位精度与稳定性（本次会话系列修复）

- **行级锁定**：字符命中只取"鼠标垂直最近的行"（baseline 聚类预计算），消除"词顶上方切上行词"误判；本行无命中时回退全局最近字符，防止高亮消失。
- **trust C（词不一致信任 C）**：C 定位词与 A 取词不一致时信任 C 的字符几何（textLayer span 错位时 A 可能取到错位文本，如 ECOSTRESS/urban 案例）。
- **文本匹配兜底**：坐标定位失败时，用取词文本在字符流中精确匹配（参考 zotero-sentence-translator 机制），仍输出字符级几何；**锚点取自 A 的 Range 在 textLayer 文本流中的非空白偏移**——重复词悬停第 N 个即高亮第 N 个，不受几何错位影响。
- **坐标转换主路径改 viewBox 比例换算**：与渲染/批注同基准，消除 `convertToPdfPoint` 系统性水平左偏（实测 px 偏左 25-197 单位）。
- **页定位修正**：页号优先取自 A 的 Range 所在 DOM 页（连续滚动下鼠标可能悬停相邻页，`currentPageNumber` 是焦点页），坐标与文本匹配始终在正确页进行。
- **词边界**：`spaceAfter`/`lineBreakAfter` 为权威断词信号（Zotero 过滤空格字符，附于前一字符标志位），rect 间距（>0.6×fontSize）仅作兜底；`isWordChar` 过滤 `undefined`/`null` 脏字符。
- **词间隙判定**：命中点须落在最近字符水平范围 ±0.25×fontSize 内才算"在词上"；间隙返回 `{gap:true}` 标记，不高亮不弹窗；窗口内无字符时不误判 gap。
- **高亮竞态防护**：per-window 递增序号保证快速移动时高亮不乱序、过期任务不复活。
- **渲染坐标基准**：C 渲染挂载到与 viewport 同源的 `pageEl`（page 局部坐标直接赋值），弹窗锚定按需还原视口坐标，消除整体偏移。
- **缩放重排**：`scalechange`/`scalechanged` 事件用缓存的 C 定位结果重算高亮与弹窗位置。
- **高亮仅限 PDF 界面**：鼠标移入左侧注释/笔记面板等非 PDF 区域自动清除高亮，仅 PDF 渲染窗口触发取词。

### 三、批注与生词本

- **批注创建跟随 C**：hover 场景加词创建高亮/下划线注释时，优先用 C 通道字符 rect（PDF 用户坐标，免去 range→PDF 转换）作批注几何，textLayer 错位不再影响批注位置。
- **旧注释便签自动补录**：打开 PDF 时自动识别本插件创建特征（单词型高亮/下划线）的注释并补录跟踪 ID，升级前创建的旧注释便签按范围正常隐藏。

### 四、界面与体验

- **翻译弹窗跟随日间/夜间模式**：以主窗口 `<window>` 的 `theme` 属性为权威信号（MutationObserver 实时同步），主题色板写入弹窗 CSS 变量（`--hte-*`），已打开弹窗即时刷新；夜间高亮改 `normal` + 半透明（避免 multiply/screen 破坏白字）。
- **偏好设置面板**：去掉高亮颜色的 "Color:" 标签；面板适配 `window[theme="dark"]`。
- **本地 HTTP 端点版本号修复**：ping 返回真实插件版本号（原误用构建环境名）。

### 五、兼容与降级

- 高亮仅在 C 数据缺失（如 Zotero 7/8 无 `getPageData`、页字符提取不全）时降级 A 的 DOM 几何，保证高亮永不缺席、不消失、不跳变。
- 调试日志前缀：`[hte-loc]`（定位）/ `[hte-noteicon]`（便签）/ `[hte-ann]`（注释），均含页码与坐标，可按前缀过滤核对。
