## v0.3.0 (2026-08-04)

### ✨ 新增

- **C 通道字符级定位**：取词（DOM 命中）与定位（PDF 字符 rect）分离，渐进增强，根治 textLayer 错位
- **文本匹配兜底**：坐标失败时按词文本精确定位，锚点取文本流偏移，重复词悬停第 N 个高亮第 N 个
- **取词高亮限定 PDF 界面**：移入注释/笔记面板自动清除高亮
- **旧注释便签自动补录**：打开 PDF 自动识别并隐藏升级前的旧注释便签

### 🔧 改进

- **viewBox 坐标换算**：消除 convertToPdfPoint 水平左偏
- **页定位修正**：以 Range 所在 DOM 页为准，连续滚动不错位
- **行级锁定 + 词不一致信任 C + 高亮竞态防护**
- **批注创建跟随 C**；弹窗跟随日间/夜间模式；ping 返回真实版本号

### 🐛 修复

- 词顶 / 词间空隙高亮消失（gap 误判）
- 重复词悬停高亮到另一个同词
- 注释面板导致 PDF 莫名高亮、旧注释便签不隐藏
- 弹窗 / 缩放重排几何漂移

### 📋 说明

- C 数据缺失（如 Zotero 7/8）时降级 A 的 DOM 几何，高亮永不缺席
- 调试日志前缀：`[hte-loc]` / `[hte-noteicon]` / `[hte-ann]`
