## v0.3.0 (2026-08-04)

### ✨ 新增

- **C 通道字符级定位**：取词（A 通道 DOM 命中）与定位（C 通道 PDF 字符 rect）分离的渐进增强——C 后台按页构建字符索引，就绪后高亮、弹窗锚定、批注几何全部跟随字符级几何，根治 textLayer 错位 / 字体 fallback 偏差
- **文本匹配兜底**：坐标定位失败时用取词文本在字符流中精确定位；锚点取自文本流偏移，重复词悬停第 N 个即高亮第 N 个
- **取词高亮限定 PDF 界面**：鼠标移入左侧注释/笔记面板等非 PDF 区域自动清除高亮
- **旧注释便签自动补录**：打开 PDF 自动识别单词型高亮/下划线注释并补录 ID，升级前旧注释便签同样正常隐藏

### 🔧 改进

- **坐标转换改 viewBox 比例**：与渲染/批注同基准，消除 convertToPdfPoint 系统性水平左偏
- **页定位修正**：以 A 的 Range 所在 DOM 页为准，连续滚动下鼠标悬停相邻页不再错位
- **行级锁定**：字符命中只取鼠标垂直最近的行，消除"词顶上方切上行词"误判
- **词不一致信任 C**：textLayer span 错位时仍用 C 字符几何渲染
- **高亮竞态防护**：快速移动时高亮不乱序、过期任务不复活
- **批注创建跟随 C**：hover 场景加词创建注释改用 C 字符 rect 定位
- **翻译弹窗跟随日间/夜间模式**：主窗口 theme 属性权威信号 + 实时刷新；夜间高亮 normal + 半透明
- **本地 HTTP 端点版本号修复**：ping 返回真实插件版本号

### 🐛 修复

- 修复词顶上方 / 词间空隙高亮消失（gap 误判）
- 修复重复词悬停高亮到另一个同词
- 修复左侧注释界面导致 PDF 出现莫名高亮
- 修复升级前旧注释便签不隐藏
- 修复弹窗 / 缩放重排几何漂移（C 通道 reflow）

### 📋 说明

- 高亮仅在 C 数据缺失（如 Zotero 7/8 无 getPageData）时降级 A 的 DOM 几何，保证高亮永不缺席
- 调试日志前缀：`[hte-loc]`（定位）/ `[hte-noteicon]`（便签）/ `[hte-ann]`（注释）
