## v0.2.9 (2026-08-01)

### ✨ 新增

- **弹窗位置选项**：在「弹窗自动关闭」上方新增「弹窗位置」下拉菜单，可选择翻译弹窗相对单词原文的位置：
  - `单词上方`（默认）：弹窗紧贴文本上方，8px 间距
  - `单词下方`：弹窗紧贴文本下方，8px 间距
- 定位规则保持不变：水平左边缘与单词左边缘对齐；空间不足自动翻转到另一侧；越界收进视口；无 Range 时回退跟随鼠标位置

### 🔧 改进

- **弹窗间距恒定**：译文/字典释义填充后弹窗自动重新定位（显式重定位 + ResizeObserver 兜底），与单词始终保持 8px 间距，多行译文不再向下延伸盖住单词
- **完整模式布局跟随弹窗方向**：字典释义区始终位于远离单词的一侧（弹窗在上→释义在上、弹窗在下→释义在下），单词/译文/+按钮 核心区永远贴近单词；翻转场景下布局与分割线方向同步切换
- **字典引擎多词典源适配**：首条释义提取支持全部 8 个词典源（必应/有道/海词/剑桥/科林斯/FreeDictionary/Weblio/Gramota），自动跳过音标行、词性行（含 `noun[C]` 变体）、英文定义行与区块标题（Weblio「意味・対訳」），剥离 `网络释义` 前缀
- **悬停翻译不再做词形还原**：字典引擎直接查询鼠标悬停的原文词（如 imager 不再被还原为 imag），词形还原仅保留在「加生词本」流程

### 🐛 修复

- 修复必应词典 were 等词简译显示 `short.` 等英文标注噪音（候选行要求中文行首优先）
- 修复悬停翻译词形还原误伤（imager→imag 查到无关词条「复数虚部」/ 查空回退显示完整条目）
- 修复 Weblio 简译「(无译文)」（标题与释义冒号分隔导致 split 第一段为空）
- 修复完整模式翻转场景分割线位置错误（分割线方向始终与实际显示位置同步）
- 修复弹窗跑 PDF 左上角严重 bug（主进程环境 `Node` 全局未定义 → 改用字面量位掩码 + 定位优先兜底）
